#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int _putchar(char c);
int main (int argc, char *argv[]);
int our_atoi(char *string, int *result);
int _atoi(const char *str);

#endif
